# Lead-Scraper-SPS

## Quick start (Windows / PowerShell)

From the repo root:

```bash
npm install
npm run dev
```

This uses the local `next` binary in `web/node_modules/.bin`, so you do not need a global install.